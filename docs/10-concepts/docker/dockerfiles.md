### Dockerfiles
A Dockerfile is a plaintext document that contains all of the commands that a user could call on the commandline to assemble an image, think of it as a script. 

#### Official Dockerfiles
Using the official docker images for these applications is generally the best approach in production, but we'll make our own to learn the format. 